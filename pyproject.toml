[project]
name = "funcypy"
version = "0.1.0"
description = "Minimalist functional leaning library"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
